import LogoutButton from "@/components/logout-button";

const Logout = () => {
  return (
    <div>
      <h1>Want to end your session?</h1>
      <LogoutButton />
    </div>
  );
};

export default Logout;
